import { StyleSheet } from 'react-native';

import { white, black, red, blue, trueBlack } from '@/utils/colors';

const styles = StyleSheet.create({
  bold: {
    fontWeight: 'bold',
  },
  white: { color: white },
  black: { color: black },
  red: { color: red },
  blue: { color: blue },
  trueBlack: { color: trueBlack },
});

export default styles;
